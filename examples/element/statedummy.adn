internal
{
    @consistency(strong) @combiner(sum) @persistence(ephemeral)
	state: Map<string, string>
}

fn init() {
}

fn req(rpc_req) {
    result := state.get(rpc_req.get('body'));
    send(rpc_req, NET);
}

fn resp(rpc_resp) {
    send(rpc_resp, APP);	
}